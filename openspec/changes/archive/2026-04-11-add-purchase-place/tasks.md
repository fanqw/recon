## 1. 数据模型与迁移

- [x] 1.1 在 Prisma schema 中新增 `PurchasePlace` 模型（进货地、市场名称、备注、软删除与时间字段）并建立与 `Order` 的关系字段。
- [x] 1.2 新增数据库迁移：创建进货地表、为订单增加 `purchase_place_id`、添加外键与未删除组合唯一索引（进货地+市场名称）。
- [x] 1.3 清空现有历史业务数据并更新 seed，写入语义化中文 mock 数据（含进货地与订单关联样本）。

## 2. 主数据与订单 API

- [x] 2.1 新增 `/api/purchase-places` 与 `/api/purchase-places/[id]` 路由，提供与分类/单位一致的列表、创建、更新、逻辑删除能力。
- [x] 2.2 在进货地创建与更新接口中统一 trim 与组合判重，并在冲突时返回 409 与可诊断错误信息。
- [x] 2.3 调整 `/api/orders` 与 `/api/orders/[id]` 的请求与响应契约，使创建/更新必须传递有效 `purchasePlaceId` 且详情可返回关联信息。

## 3. 删除保护与错误语义

- [x] 3.1 为分类删除接口增加“被未删除商品引用”检查，命中时拒绝删除并返回冲突错误。
- [x] 3.2 为单位删除接口增加“被未删除商品引用”检查，命中时拒绝删除并返回冲突错误。
- [x] 3.3 为商品删除接口增加“被未删除订单明细引用”检查，命中时拒绝删除并返回冲突错误。
- [x] 3.4 为进货地删除接口增加“被未删除订单引用”检查，命中时拒绝删除并返回冲突错误。
- [x] 3.5 定义并落地删除失败错误码枚举（`CATEGORY_IN_USE`、`UNIT_IN_USE`、`COMMODITY_IN_USE`、`PURCHASE_PLACE_IN_USE`）及统一响应结构。

## 4. 前端页面与交互

- [x] 4.1 新增进货地管理页面（基础资料入口）并接入增删改查交互。
- [x] 4.2 在导航中增加进货地入口，确保与现有基础资料导航一致。
- [x] 4.3 调整订单新建表单：新增进货地下拉并设为必填，提交请求包含 `purchasePlaceId`。
- [x] 4.4 在订单列表/详情展示订单关联的进货地信息，并按错误码枚举映射删除受阻提示。

## 5. 测试与文档

- [x] 5.1 扩展 API 集成测试：覆盖进货地 CRUD、组合唯一冲突、订单必填进货地、四类删除拦截场景及错误码断言。
- [x] 5.2 扩展 E2E：覆盖进货地导航与基础交互、删除受阻提示可见性。
- [x] 5.3 更新 README 与相关文档，补充新增模块、接口约束与测试命令说明。
