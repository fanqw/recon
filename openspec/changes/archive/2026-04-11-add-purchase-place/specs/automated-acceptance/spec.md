## MODIFIED Requirements

### Requirement: 核心 API 的自动化验证
系统 SHALL 提供可在本地与 CI 中重复执行的自动化测试，验证主要 HTTP API（或等价服务端入口）在已认证与未认证场景下的行为符合 `admin-authentication`、`master-data`、`sales-orders` 中的关键路径，并覆盖进货地与删除拦截场景。

#### Scenario: 未认证访问受保护 API 失败
- **WHEN** 测试在未携带会话的情况下调用受保护的业务 API
- **THEN** 测试 SHALL 断言响应为未授权（如 401/403），且套件通过

#### Scenario: 认证后主数据与订单 API 可通（含进货地）
- **WHEN** 测试在有效会话下依次调用分类、单位、商品、进货地、订单及订单相关 API 的最小成功路径
- **THEN** 测试 SHALL 断言 HTTP 成功状态与响应体中存在预期标识或字段，且套件通过

#### Scenario: 已关联主数据删除被拦截
- **WHEN** 测试在有效会话下删除被未删除记录关联的分类、单位、商品或进货地
- **THEN** 测试 SHALL 断言接口返回冲突错误（如 409），且原记录仍保持未删除状态

### Requirement: 关键页面的浏览器自动化
系统 SHALL 提供浏览器端端到端测试，验证登录页与主要业务页面上的可交互控件（按钮、链接、表单提交）可被操作，且主流程无阻塞性错误（与实现阶段选定的框架一致，推荐 Playwright）。

#### Scenario: 登录页可交互
- **WHEN** E2E 启动应用并打开登录页
- **THEN** 测试 SHALL 能够填充用户名与密码字段并触发提交控件，且不因前端错误导致流程中断

#### Scenario: 登录后主导航可达且基础页可点击（含进货地）
- **WHEN** E2E 使用有效凭据完成登录
- **THEN** 测试 SHALL 能够导航至分类、单位、商品、进货地、订单列表及至少一个订单详情相关路径，并对每页至少一个主要操作控件执行点击（或等价交互）且页面无未捕获致命错误

#### Scenario: 删除受阻时页面可提示
- **WHEN** E2E 在页面中尝试删除被未删除记录关联的主数据
- **THEN** 测试 SHALL 能够观察到失败提示（或等效错误反馈），且页面状态与数据保持一致
